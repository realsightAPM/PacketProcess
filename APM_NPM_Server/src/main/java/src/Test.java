package src;

import java.util.Map;

import org.dom4j.DocumentException;

import src.globalinfo.ConfigLoad;
import src.globalinfo.ProtocolServer;
import src.globalinfo.ServerInfo;


public class Test {

	public static void main(String[] args) throws DocumentException {
	/*	ProtocolServer ps = ConfigLoad.loadProtocolServerFromXml("/Users/zyd/apm/npm/APM_NPM_Server/protocol_application.xml");
		Map<String, ServerInfo> map = ps.getMap();
		map.forEach((k,v)->{
			System.out.println(k);
			Map<String,String> fields = v.getFieldMap();
			fields.forEach((k1,v1)->{
				System.out.println(k1+" &&&   "+v1);
			});
		});*/
		long end_time = 1504890061000L;
		long start_time = 1483203661000L;
 	}

}
